{
  "type": "function",
  "name": "find_common_slots",
  "description": "Leer agendas locales y proponer horarios comunes, sin crear eventos.",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {
      "scenario": {
        "type": "string",
        "enum": [
          "01",
          "02",
          "03"
        ]
      }
    },
    "required": [
      "scenario"
    ],
    "additionalProperties": false
  }
}
